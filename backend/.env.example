# Copie para .env e ajuste conforme necessario
SECRET_KEY=django-insecure-troque-esta-chave-em-producao
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (docker compose up -d db)
DATABASE_URL=postgres://agendamento:agendamento@localhost:5434/agendamento

# Origens liberadas no CORS (frontend Vite)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
